using UnityEngine;

public class GraphNode
{
    public int nodeID;
    public bool isActive;
    public bool isRoot;

    public GraphNode()
    { } // Setting the first node as root

    public GraphNode(int nodeID,bool isActive ,bool isRoot)
    { } // Setting the first node as root
}
